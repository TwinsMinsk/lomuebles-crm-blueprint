
import { useMutation, useQueryClient } from "@tanstack/react-query";
import { supabase } from "@/integrations/supabase/client";
import { toast } from "sonner";
import { Order } from "@/types/order";

// Define the type for orderData that matches Supabase's requirements
type OrderInput = {
  client_contact_id?: number | null; // Now optional
  client_language: string; 
  order_type: string;
  status: string;
  creator_user_id: string;
  client_company_id?: number | null;
  source_lead_id?: number | null;
  assigned_user_id?: string | null;
  partner_manufacturer_id?: number | null;
  final_amount?: number | null;
  payment_status?: string | null;
  delivery_address_full?: string | null;
  notes_history?: string | null;
  attached_files_order_docs?: any[] | null;
  closing_date?: string | null;
  order_name?: string | null;
  // We don't include order_number field here as it's generated by the database
};

export const addOrder = async (orderData: OrderInput) => {
  // Note: order_number is not included in the input - it will be generated by the database trigger
  const { data, error } = await supabase
    .from('orders')
    .insert(orderData as any)
    .select();
  
  if (error) throw error;
  return data;
};

export const useAddOrder = () => {
  const queryClient = useQueryClient();
  
  const mutation = useMutation({
    mutationFn: addOrder,
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['orders'] });
      toast.success('Заказ успешно создан!');
    },
    onError: (err: any) => {
      toast.error(`Ошибка создания заказа: ${err.message}`);
    }
  });

  return {
    addOrder: mutation.mutate,
    isLoading: mutation.isPending,
    error: mutation.error
  };
};
